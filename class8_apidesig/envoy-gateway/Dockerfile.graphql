FROM python:3.11-slim

WORKDIR /app

# Copy GraphQL files
COPY ../graphql-demo/*.py /app/
COPY ../graphql-demo/requirements.txt /app/

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
